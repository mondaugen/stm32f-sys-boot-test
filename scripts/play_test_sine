# Plays a sine wave on the right channel of the given AUDIODEV
export AUDIODEV=plughw:CARD=Pro,DEV=0
sox --combine merge "|sox -n -c 1 -p synth sine 1 gain -200"\
    "|sox -n -c 1 -p synth sine 440" -d
